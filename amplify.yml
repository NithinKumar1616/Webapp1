version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd App
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: App/dist
    files:
      - '**/*'
  cache:
    paths:
      - App/node_modules/**/*

